# Dependencies
/node_modules
/.pnp
.pnp.js

# Build and coverage
/.next/
/out/
/.swc/
/build
/dist
/coverage
/.nyc_output
*.tsbuildinfo
next-env.d.ts

# Environment files
.env*.local
.env
.env.production
.env.development
.env.test

# Local editor and tool state
.vscode/
.idea/
*.sublime-project
*.sublime-workspace
*.swp
*.swo
*~
.claude/
.cursor/
.playwright-cli/
.vercel/
.tmp/

# Runtime scratch and generated artifacts
_temp/
logs/
tmp/
/.tmp-next-start.log
/.tmp-next-start.pid
/*.log
/*.pid
_temp/reports/dispatch/

# Misc local files
.DS_Store
Thumbs.db
Desktop.ini
*.pem
*.tmp
*.temp
*.cache
.cache/

# next-pwa 생성물 (dest: "public")
# 빌드마다 새로 만들어지고, 안에 그 빌드의 청크 해시가 박힌 precache 목록이 들어간다.
# 추적하면 빌드할 때마다 작업 트리가 더러워지고, 커밋된 사본은 다른 환경(Vercel)의
# 빌드 결과와 어긋난 URL을 가리키게 된다. next-pwa 권장대로 무시한다.
# public/sw.js 는 이제 손으로 작성하는 소스다 (next-pwa 생성물이던 시절의
# ignore 가 남아 커밋을 막았고, 프로덕션에 서비스워커가 배포되지 않았다)
public/workbox-*.js
public/workbox-*.js.map
