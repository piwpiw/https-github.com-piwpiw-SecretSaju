# Page Improvements (Admin Audit)

- Generated at: `2026-03-05T07:11:09.861Z`
- Scope: `63` pages under `src/app/**/page.tsx`
- Rule: each page includes exactly 10 improvement items.

## /
- File: `src/app/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P3` 키보드 탐색 개선: 탭 순서와 포커스 링 대비를 WCAG 기준으로 정리.
7. `P2` 문서 구조 개선: 페이지당 단일 `h1`을 두고 섹션 계층을 재정렬.
8. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /about
- File: `src/app/about/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /admin
- File: `src/app/admin/page.tsx`
1. `P1` 관리자 RBAC 가드 강화: 비관리자 접근 시 즉시 리다이렉트 + 감사 로그를 남기도록 보강.
2. `P1` 관리자 주요 액션에 확인 단계 추가: 실수 클릭 방지를 위해 확인 모달과 취소 경로를 명확화.
3. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
4. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
5. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
6. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /admin/advanced-scoring
- File: `src/app/admin/advanced-scoring/page.tsx`
1. `P1` 관리자 RBAC 가드 강화: 비관리자 접근 시 즉시 리다이렉트 + 감사 로그를 남기도록 보강.
2. `P1` 관리자 주요 액션에 확인 단계 추가: 실수 클릭 방지를 위해 확인 모달과 취소 경로를 명확화.
3. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
4. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
5. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
6. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
7. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
8. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
9. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
10. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.

## /admin/character-analysis
- File: `src/app/admin/character-analysis/page.tsx`
1. `P1` 관리자 RBAC 가드 강화: 비관리자 접근 시 즉시 리다이렉트 + 감사 로그를 남기도록 보강.
2. `P1` 관리자 주요 액션에 확인 단계 추가: 실수 클릭 방지를 위해 확인 모달과 취소 경로를 명확화.
3. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
4. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
5. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
6. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
7. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
8. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
9. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
10. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.

## /admin/character-profile
- File: `src/app/admin/character-profile/page.tsx`
1. `P1` 관리자 RBAC 가드 강화: 비관리자 접근 시 즉시 리다이렉트 + 감사 로그를 남기도록 보강.
2. `P1` 관리자 주요 액션에 확인 단계 추가: 실수 클릭 방지를 위해 확인 모달과 취소 경로를 명확화.
3. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
4. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
5. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
6. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
7. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
8. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
9. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
10. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.

## /admin/compatibility
- File: `src/app/admin/compatibility/page.tsx`
1. `P1` 관리자 RBAC 가드 강화: 비관리자 접근 시 즉시 리다이렉트 + 감사 로그를 남기도록 보강.
2. `P1` 관리자 주요 액션에 확인 단계 추가: 실수 클릭 방지를 위해 확인 모달과 취소 경로를 명확화.
3. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
4. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
5. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
6. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
7. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
8. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
9. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
10. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.

## /admin/test-control
- File: `src/app/admin/test-control/page.tsx`
1. `P1` 관리자 RBAC 가드 강화: 비관리자 접근 시 즉시 리다이렉트 + 감사 로그를 남기도록 보강.
2. `P1` 관리자 주요 액션에 확인 단계 추가: 실수 클릭 방지를 위해 확인 모달과 취소 경로를 명확화.
3. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
4. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
5. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
6. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
7. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
8. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
9. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
10. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.

## /analysis-history
- File: `src/app/analysis-history/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P3` 정보 밀도 개선: 카드/배지/요약문으로 핵심 정보를 빠르게 훑도록 구성.
9. `P2` 이벤트 계측 추가: 진입, CTA 클릭, 이탈 지점을 페이지 단위로 수집.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /analysis-history/[type]/[id]
- File: `src/app/analysis-history/[type]/[id]/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P3` 키보드 탐색 개선: 탭 순서와 포커스 링 대비를 WCAG 기준으로 정리.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P2` 이벤트 계측 추가: 진입, CTA 클릭, 이탈 지점을 페이지 단위로 수집.

## /astrology
- File: `src/app/astrology/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /auth/callback
- File: `src/app/auth/callback/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
8. `P3` 정보 밀도 개선: 카드/배지/요약문으로 핵심 정보를 빠르게 훑도록 구성.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /blog
- File: `src/app/blog/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P3` 정보 밀도 개선: 카드/배지/요약문으로 핵심 정보를 빠르게 훑도록 구성.
9. `P2` 이벤트 계측 추가: 진입, CTA 클릭, 이탈 지점을 페이지 단위로 수집.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /calendar
- File: `src/app/calendar/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /compatibility
- File: `src/app/compatibility/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /consultation
- File: `src/app/consultation/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /custom/partnership
- File: `src/app/custom/partnership/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P3` 키보드 탐색 개선: 탭 순서와 포커스 링 대비를 WCAG 기준으로 정리.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /daily
- File: `src/app/daily/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /dashboard
- File: `src/app/dashboard/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /destiny
- File: `src/app/destiny/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /dreams
- File: `src/app/dreams/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /encyclopedia
- File: `src/app/encyclopedia/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /faq
- File: `src/app/faq/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P2` 이벤트 계측 추가: 진입, CTA 클릭, 이탈 지점을 페이지 단위로 수집.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /fortune
- File: `src/app/fortune/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /gift
- File: `src/app/gift/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P3` 키보드 탐색 개선: 탭 순서와 포커스 링 대비를 WCAG 기준으로 정리.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
9. `P3` 정보 밀도 개선: 카드/배지/요약문으로 핵심 정보를 빠르게 훑도록 구성.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /healing
- File: `src/app/healing/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /history
- File: `src/app/history/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P3` 키보드 탐색 개선: 탭 순서와 포커스 링 대비를 WCAG 기준으로 정리.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /inquiry
- File: `src/app/inquiry/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P3` 키보드 탐색 개선: 탭 순서와 포커스 링 대비를 WCAG 기준으로 정리.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
9. `P3` 정보 밀도 개선: 카드/배지/요약문으로 핵심 정보를 빠르게 훑도록 구성.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /legal
- File: `src/app/legal/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P3` 정보 밀도 개선: 카드/배지/요약문으로 핵심 정보를 빠르게 훑도록 구성.
9. `P2` 이벤트 계측 추가: 진입, CTA 클릭, 이탈 지점을 페이지 단위로 수집.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /login
- File: `src/app/login/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P3` 정보 밀도 개선: 카드/배지/요약문으로 핵심 정보를 빠르게 훑도록 구성.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /luck
- File: `src/app/luck/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /more
- File: `src/app/more/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /my-saju/add
- File: `src/app/my-saju/add/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P3` 키보드 탐색 개선: 탭 순서와 포커스 링 대비를 WCAG 기준으로 정리.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P2` 이벤트 계측 추가: 진입, CTA 클릭, 이탈 지점을 페이지 단위로 수집.

## /my-saju/list
- File: `src/app/my-saju/list/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P3` 키보드 탐색 개선: 탭 순서와 포커스 링 대비를 WCAG 기준으로 정리.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /mypage
- File: `src/app/mypage/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /naming
- File: `src/app/naming/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P3` 키보드 탐색 개선: 탭 순서와 포커스 링 대비를 WCAG 기준으로 정리.
7. `P2` 문서 구조 개선: 페이지당 단일 `h1`을 두고 섹션 계층을 재정렬.
8. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /palmistry
- File: `src/app/palmistry/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P3` 키보드 탐색 개선: 탭 순서와 포커스 링 대비를 WCAG 기준으로 정리.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /payment/fail
- File: `src/app/payment/fail/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P2` 문서 구조 개선: 페이지당 단일 `h1`을 두고 섹션 계층을 재정렬.
7. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
8. `P3` 정보 밀도 개선: 카드/배지/요약문으로 핵심 정보를 빠르게 훑도록 구성.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /payment/loading
- File: `src/app/payment/loading/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P2` 이벤트 계측 추가: 진입, CTA 클릭, 이탈 지점을 페이지 단위로 수집.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /payment/success
- File: `src/app/payment/success/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P3` 키보드 탐색 개선: 탭 순서와 포커스 링 대비를 WCAG 기준으로 정리.
6. `P2` 문서 구조 개선: 페이지당 단일 `h1`을 두고 섹션 계층을 재정렬.
7. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
8. `P3` 정보 밀도 개선: 카드/배지/요약문으로 핵심 정보를 빠르게 훑도록 구성.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /privacy
- File: `src/app/privacy/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /psychology
- File: `src/app/psychology/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /psychology/module
- File: `src/app/psychology/module/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P3` 정보 밀도 개선: 카드/배지/요약문으로 핵심 정보를 빠르게 훑도록 구성.
9. `P2` 이벤트 계측 추가: 진입, CTA 클릭, 이탈 지점을 페이지 단위로 수집.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /psychology/module/[id]
- File: `src/app/psychology/module/[id]/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /psychology/premium-report
- File: `src/app/psychology/premium-report/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
8. `P3` 정보 밀도 개선: 카드/배지/요약문으로 핵심 정보를 빠르게 훑도록 구성.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /refund
- File: `src/app/refund/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /relationship
- File: `src/app/relationship/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P3` 키보드 탐색 개선: 탭 순서와 포커스 링 대비를 WCAG 기준으로 정리.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P2` 이벤트 계측 추가: 진입, CTA 클릭, 이탈 지점을 페이지 단위로 수집.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /relationship/[id]
- File: `src/app/relationship/[id]/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P2` 문서 구조 개선: 페이지당 단일 `h1`을 두고 섹션 계층을 재정렬.
7. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /relationship/[id]/vs
- File: `src/app/relationship/[id]/vs/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /result/[token]
- File: `src/app/result/[token]/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
9. `P3` 정보 밀도 개선: 카드/배지/요약문으로 핵심 정보를 빠르게 훑도록 구성.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /saju
- File: `src/app/saju/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /select-fortune
- File: `src/app/select-fortune/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P1` 폼 검증 강화: 필수값/형식/길이 검증을 클라이언트와 서버 양쪽에서 일치시키기.
5. `P2` 폼 접근성 개선: 입력마다 `label`, `aria-describedby`, 오류 포커스 이동 지원.
6. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
7. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
8. `P2` 데이터 요청 캐시 정책 정리: 중복 fetch를 줄이고 stale 상태를 명확히 처리.
9. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
10. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.

## /shinsal
- File: `src/app/shinsal/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /shop
- File: `src/app/shop/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /signup
- File: `src/app/signup/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P3` 정보 밀도 개선: 카드/배지/요약문으로 핵심 정보를 빠르게 훑도록 구성.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /story
- File: `src/app/story/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /story/[id]
- File: `src/app/story/[id]/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P3` 키보드 탐색 개선: 탭 순서와 포커스 링 대비를 WCAG 기준으로 정리.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P3` 정보 밀도 개선: 카드/배지/요약문으로 핵심 정보를 빠르게 훑도록 구성.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /support
- File: `src/app/support/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /tarot
- File: `src/app/tarot/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /terms
- File: `src/app/terms/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P3` 정보 밀도 개선: 카드/배지/요약문으로 핵심 정보를 빠르게 훑도록 구성.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /tojeong
- File: `src/app/tojeong/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P2` 로딩 UX 정교화: 지연 500ms 이상 구간에 단계형 로딩 메시지를 노출.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /wiki
- File: `src/app/wiki/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P1` 오류 상태 UI 추가: 네트워크 실패/권한 실패/빈 응답을 각각 안내.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P3` 이벤트 네이밍 통일: 분석 대시보드에서 페이지 비교가 가능하도록 스키마 정리.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.

## /wiki/[slug]
- File: `src/app/wiki/[slug]/page.tsx`
1. `P1` 인증 상태 분기 고도화: 로그인/비로그인 상태에서 CTA를 분리해 잘못된 진입을 줄이기.
2. `P1` 로딩 상태 명시: 첫 진입/재요청 시 Skeleton 또는 Progress UI를 추가.
3. `P2` 오류 복구 액션 개선: 재시도 버튼과 지원 채널 링크를 오류 박스에 통합.
4. `P2` 주요 CTA 검증: 클릭 후 성공/실패 토스트 및 상태 변화를 일관되게 표시.
5. `P2` 접근성 속성 보강: 핵심 인터랙션 요소에 `aria-label`과 landmark role 부여.
6. `P3` 헤딩 카피 최적화: 검색 의도와 일치하는 제목/서브카피로 개선.
7. `P3` 초기 데이터 프리패치 검토: 사용 빈도가 높은 경로에 선로딩 적용.
8. `P2` 빈 목록 UX 추가: 데이터가 없을 때 다음 액션으로 연결되는 Empty State 제공.
9. `P2` 이벤트 계측 추가: 진입, CTA 클릭, 이탈 지점을 페이지 단위로 수집.
10. `P2` 모바일 최적화 점검: 360px 뷰포트 기준으로 터치 타겟 44px 이상 유지.
