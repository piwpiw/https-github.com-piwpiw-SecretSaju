# test-gen

Use for creating or extending automated tests.

## Workflow
1. Classify the change as `logic`, `routes`, `ui`, `validation`, or `fixtures`.
2. Place the test under `tests/` only.
3. Match existing naming and setup patterns.
4. Run the narrowest useful test command first.
