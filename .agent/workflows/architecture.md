# Architecture Workflow

1. Confirm public contracts that must stay stable.
2. Propose the target structure by domain and ownership.
3. Move code in small batches.
4. Fix imports immediately after each batch.
5. Run lint, typecheck, and relevant tests between batches.
